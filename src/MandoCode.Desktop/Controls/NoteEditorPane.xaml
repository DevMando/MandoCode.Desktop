<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MandoCode.Desktop.Controls.NoteEditorPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- One open note. Fills the docked Notes panel, replacing the list while a note is open (at
         panel width there isn't room for both, and writing wants the whole column). The note file
         is the only copy of what you type: the TextBox autosaves on a debounce, and every save,
         rename, and agent hand-off is logged to the note's shadow sidecar. -->
    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header: back to the list, the note's name (click-to-rename in place), and its menu. -->
        <Grid Grid.Row="0" Padding="10,10,10,6" ColumnSpacing="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="BackButton" Click="Back_Click" Padding="6"
                    Background="Transparent" BorderThickness="0" VerticalAlignment="Top"
                    AutomationProperties.Name="Back to all notes"
                    ToolTipService.ToolTip="All notes">
                <FontIcon Glyph="&#xE72B;" FontSize="13"/>
            </Button>

            <StackPanel Grid.Column="1" Spacing="1">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock x:Name="TitleText" FontSize="14" FontWeight="SemiBold"
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    <!-- Unsaved marker. Autosave means this is only ever up for a second or two,
                         but while it's up the disk does not yet have what you typed. -->
                    <Ellipse x:Name="DirtyDot" Width="6" Height="6" Visibility="Collapsed"
                             VerticalAlignment="Center"
                             Fill="{StaticResource MandoGoldBrush}"
                             ToolTipService.ToolTip="Unsaved — autosaving"/>
                </StackPanel>
                <!-- Rename in place: no dialog for something this small. Enter commits, Esc cancels. -->
                <TextBox x:Name="RenameBox" Visibility="Collapsed" FontSize="14"
                         KeyDown="RenameBox_KeyDown" LostFocus="RenameBox_LostFocus"
                         PlaceholderText="Note name"/>
                <TextBlock x:Name="GroupText" FontSize="11" Opacity="0.55"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2" VerticalAlignment="Top">
            <!-- Undo/Redo the assistant's last Insert/Replace — a single-level TOGGLE between the
                 buffer right before that edit and right after it (see NoteEditorPane's undo/redo
                 region). Exactly one of the two is ever visible, matching whichever endpoint the
                 editor's current text equals; both go away once you type past either endpoint.
                 They exist because Ctrl+Z can't do this job: assigning Editor.Text resets the
                 TextBox's own undo history, so the one edit the user didn't type by hand is the one
                 the control can't reverse on its own. Gold, like the unsaved dot: this is the note
                 in a state worth noticing. -->
            <Button x:Name="UndoButton" Click="Undo_Click" Padding="6" Visibility="Collapsed"
                    Background="Transparent" BorderThickness="0"
                    AutomationProperties.Name="Undo the assistant's insert"
                    ToolTipService.ToolTip="Undo the assistant's insert">
                <FontIcon Glyph="&#xE7A7;" FontSize="13"
                          Foreground="{StaticResource MandoGoldBrush}"/>
            </Button>
            <Button x:Name="RedoButton" Click="Redo_Click" Padding="6" Visibility="Collapsed"
                    Background="Transparent" BorderThickness="0"
                    AutomationProperties.Name="Redo the assistant's insert"
                    ToolTipService.ToolTip="Redo the assistant's insert">
                <FontIcon Glyph="&#xE7A6;" FontSize="13"
                          Foreground="{StaticResource MandoGoldBrush}"/>
            </Button>

            <Button Padding="6" Background="Transparent" BorderThickness="0"
                    VerticalAlignment="Top" AutomationProperties.Name="Note options"
                    ToolTipService.ToolTip="Note options">
                <FontIcon Glyph="&#xE712;" FontSize="13"/>
                <Button.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedRight">
                        <MenuFlyoutItem Text="Rename" Click="Rename_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE8AC;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Show in Explorer" Click="Reveal_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE838;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Delete" Click="Delete_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE74D;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
            </StackPanel>
        </Grid>

        <!-- Conflict surface. Something other than this editor wrote (or removed) the note —
             an approved agent edit, Notepad, git. Never resolved silently while there are
             unsaved keystrokes: whichever side we picked for you would be the wrong one. -->
        <InfoBar x:Name="ConflictBar" Grid.Row="1" Margin="10,0,10,6"
                 IsOpen="False" IsClosable="False" Severity="Warning">
            <!-- Both choices live in Content rather than ActionButton (which holds a single
                 ButtonBase) so neither resolution is styled as the obvious one. -->
            <InfoBar.Content>
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,10">
                    <Button x:Name="ConflictPrimary" Click="ConflictPrimary_Click" FontSize="12" Padding="10,3"/>
                    <Button x:Name="ConflictSecondary" Click="ConflictSecondary_Click" FontSize="12" Padding="10,3"/>
                </StackPanel>
            </InfoBar.Content>
        </InfoBar>

        <!-- The note itself. Deliberately plain: no toolbar, no markdown preview, no formatting -
             it's a notepad. Spellcheck is off because notes carry paths, commands, and identifiers. -->
        <TextBox x:Name="Editor" Grid.Row="2" Margin="10,0,10,0"
                 AcceptsReturn="True" TextWrapping="Wrap" IsSpellCheckEnabled="False"
                 FontSize="13" Padding="10,8"
                 VerticalAlignment="Stretch" VerticalContentAlignment="Top"
                 Background="{StaticResource MandoBackgroundBrush}"
                 BorderBrush="{StaticResource MandoBorderBrush}"
                 PlaceholderText="Start typing…"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 TextChanged="Editor_TextChanged" KeyDown="Editor_KeyDown"
                 AutomationProperties.Name="Note text"/>

        <!-- Status: what the disk knows. "Saved 2:14 PM" is the whole reassurance this feature owes
             you, since nothing here has a Save button. -->
        <Grid Grid.Row="3" Padding="12,6,12,10" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" FontSize="11" Opacity="0.6"
                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            <TextBlock x:Name="SizeText" Grid.Column="1" FontSize="11" Opacity="0.45"
                       VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
