<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MandoCode.Desktop.ChatTabView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MandoCode.Desktop"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:MandoCode.Desktop.Controls">

    <Grid x:Name="ChatRoot" SizeChanged="ChatRoot_SizeChanged"
          AllowDrop="True" DragEnter="ChatRoot_DragEnter" DragLeave="ChatRoot_DragLeave">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ======= Per-tab header =======
             Connection, model, tokens, project folder, and the folder action all
             describe THIS agent. Each tab has its own, because each tab has its own
             AIService, TokenTrackingService, and ProjectRootAccessor. -->
        <Grid Grid.Row="0" Padding="16,8,16,8" Background="{StaticResource MandoPanelBrush}"
              ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <Ellipse x:Name="ConnectionDot" Width="10" Height="10" Fill="Gray" VerticalAlignment="Center"/>

                <!-- Clicking the model opens a quick-switch dropdown anchored to the button.
                     It's a tab-local switch — it never touches the saved default. (The typed
                     /model command still uses the overlay wizard.) -->
                <Button x:Name="ModelButton" Padding="6,3"
                        Background="Transparent" BorderThickness="0"
                        ToolTipService.ToolTip="Switch the model for this tab only (doesn't change the saved default)">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock x:Name="ModelText" Text="" VerticalAlignment="Center"
                                   Foreground="{StaticResource MandoSkyBrush}" FontSize="13"/>
                        <FontIcon Glyph="&#xE70D;" FontSize="10" Opacity="0.6" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button.Flyout>
                        <Flyout x:Name="ModelFlyout" Placement="Bottom" Opening="ModelFlyout_Opening">
                            <Flyout.FlyoutPresenterStyle>
                                <Style TargetType="FlyoutPresenter">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="MinWidth" Value="260"/>
                                    <Setter Property="MaxWidth" Value="380"/>
                                    <Setter Property="MaxHeight" Value="400"/>
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </Flyout.FlyoutPresenterStyle>
                            <Grid MinWidth="260">
                                <!-- Loading state: shown immediately while models are fetched. -->
                                <StackPanel x:Name="ModelLoadingPanel" Orientation="Horizontal" Spacing="10"
                                            Padding="16,16" VerticalAlignment="Center">
                                    <ProgressRing IsActive="True" Width="18" Height="18"/>
                                    <TextBlock Text="Loading models…" VerticalAlignment="Center" Opacity="0.7"/>
                                </StackPanel>
                                <!-- Error state (unreachable Ollama, empty list, …). -->
                                <TextBlock x:Name="ModelErrorText" Visibility="Collapsed" TextWrapping="Wrap"
                                           Padding="16,14" Opacity="0.85" MaxWidth="340"/>
                                <!-- Loaded list — cloud first, badge per row; click to switch. -->
                                <ListView x:Name="ModelList" Visibility="Collapsed"
                                          SelectionMode="Single" IsItemClickEnabled="True"
                                          ItemClick="ModelList_ItemClick" Padding="4,6">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Padding" Value="10,7"/>
                                            <Setter Property="MinHeight" Value="0"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="local:ModelItem">
                                            <Grid ColumnSpacing="12" MinWidth="220">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center"
                                                           TextTrimming="CharacterEllipsis" FontSize="13"/>
                                                <Border Grid.Column="1" CornerRadius="6" Padding="7,1"
                                                        VerticalAlignment="Center"
                                                        Background="{x:Bind BadgeBackground}">
                                                    <TextBlock Text="{x:Bind Badge}" FontSize="11"
                                                               Foreground="{x:Bind BadgeForeground}"/>
                                                </Border>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </Flyout>
                    </Button.Flyout>
                </Button>

                <TextBlock x:Name="TokenText" Text="" VerticalAlignment="Center" Opacity="0.6" FontSize="12"/>
            </StackPanel>

            <TextBlock x:Name="ProjectRootText" Grid.Column="1" Text="" VerticalAlignment="Center"
                       HorizontalAlignment="Right" Opacity="0.6" FontSize="12"
                       TextTrimming="CharacterEllipsis" Margin="16,0,8,0"
                       ToolTipService.ToolTip="This tab's project folder"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6">
                <Button x:Name="OpenFolderButton" Click="OpenFolderButton_Click" Padding="8,5"
                        ToolTipService.ToolTip="Change this tab's project folder">
                    <FontIcon Glyph="&#xE8B7;" FontSize="15"/>
                </Button>
                <Button x:Name="ExplorerButton" Click="ExplorerButton_Click" Padding="8,5"
                        ToolTipService.ToolTip="Show or hide the file explorer">
                    <FontIcon Glyph="&#xEC50;" FontSize="15"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Transcript row: the explorer panel DOCKS in the second column (like the snapshot
             list docks beside the chat), so opening it resizes the transcript instead of
             covering its text. Column width is set in code (~20% of the window, clamped);
             Collapsed removes it from layout entirely and the transcript reclaims the width. -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <WebView2 x:Name="TranscriptView" DefaultBackgroundColor="#16121F"/>

            <!-- Drag splitter for the explorer: ResizeGrip shows the ↔ cursor on hover; the
                 drag itself is pointer events in code-behind (same pattern as the terminal's
                 splitter in MainWindow). Shown/hidden together with the panel. -->
            <controls:ResizeGrip x:Name="ExplorerSplitter" Grid.Column="1" Width="6"
                                 Visibility="Collapsed" Background="Transparent"
                                 GripOrientation="Vertical"
                                 PointerPressed="ExplorerSplitter_PointerPressed"
                                 PointerMoved="ExplorerSplitter_PointerMoved"
                                 PointerReleased="ExplorerSplitter_PointerReleased"/>

            <!-- ======= File explorer panel (solution-explorer style) =======
                 Toggled by the header's explorer button. The tree is LAZY: a folder's children
                 are only read from disk when it is first expanded, so opening the panel never
                 scans node_modules/bin/obj unless you drill into them. -->
            <Grid x:Name="ExplorerPanel" Grid.Column="2" Visibility="Collapsed" Width="280"
                  Background="{StaticResource MandoPanelBrush}"
                  BorderBrush="{StaticResource MandoBorderBrush}" BorderThickness="1,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Padding="12,8,6,8" ColumnSpacing="8"
                      BorderBrush="{StaticResource MandoBorderBrush}" BorderThickness="0,0,0,1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xEC50;" FontSize="14" VerticalAlignment="Center"
                              Foreground="{StaticResource MandoAccentBrush}"/>
                    <TextBlock x:Name="ExplorerRootText" Grid.Column="1" FontWeight="SemiBold"
                               FontSize="13" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Column="2" Click="ExplorerRefresh_Click" Padding="6"
                            Background="Transparent" BorderThickness="0"
                            ToolTipService.ToolTip="Refresh — re-read the folder tree from disk">
                        <FontIcon Glyph="&#xE72C;" FontSize="12"/>
                    </Button>
                    <Button Grid.Column="3" Click="ExplorerClose_Click" Padding="6"
                            Background="Transparent" BorderThickness="0"
                            ToolTipService.ToolTip="Close the file explorer">
                        <FontIcon Glyph="&#xE711;" FontSize="12"/>
                    </Button>
                </Grid>

                <!-- Files / Changes tab strip. The active tab is semibold + full opacity
                     (styling flipped in code — WinUI styles have no triggers). -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="2" Padding="8,4,8,2">
                    <Button x:Name="FilesTabButton" Content="Files" Click="FilesTab_Click"
                            FontSize="12" FontWeight="SemiBold" Padding="8,3"
                            Background="Transparent" BorderThickness="0"/>
                    <Button x:Name="ChangesTabButton" Content="Changes" Click="ChangesTab_Click"
                            FontSize="12" Padding="8,3" Opacity="0.55"
                            Background="Transparent" BorderThickness="0"/>
                </StackPanel>

                <TreeView x:Name="ExplorerTree" Grid.Row="2" SelectionMode="Single"
                          CanDragItems="True" CanReorderItems="False" AllowDrop="False"
                          DragItemsStarting="ExplorerTree_DragItemsStarting"
                          Expanding="ExplorerTree_Expanding" ItemInvoked="ExplorerTree_ItemInvoked"
                          DoubleTapped="ExplorerTree_DoubleTapped"
                          Padding="4,6,4,6">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="MinHeight" Value="26"/>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <DataTemplate x:DataType="muxc:TreeViewNode">
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <FontIcon Glyph="{x:Bind ((local:ExplorerItem)Content).Glyph}"
                                          FontSize="13" VerticalAlignment="Center"
                                          Foreground="{x:Bind ((local:ExplorerItem)Content).IconBrush}"/>
                                <TextBlock Grid.Column="1"
                                           Text="{x:Bind ((local:ExplorerItem)Content).Name}"
                                           FontSize="13" VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>
                                <!-- Gold dot: this file changed / this folder contains changes.
                                     OneWay — flags update in place on every git refresh. -->
                                <Ellipse Grid.Column="2" Width="6" Height="6" VerticalAlignment="Center"
                                         Fill="{StaticResource MandoGoldBrush}"
                                         Visibility="{x:Bind ((local:ExplorerItem)Content).DirtyVisibility, Mode=OneWay}"
                                         ToolTipService.ToolTip="Uncommitted changes"/>
                                <!-- Tag button: inserts this row's @token into the prompt, same as
                                     dragging it there. Dim at rest, brightens on hover (code-behind —
                                     WinUI styles have no triggers). -->
                                <Button Grid.Column="3" Click="ExplorerTag_Click"
                                        PointerEntered="ExplorerTag_PointerEntered"
                                        PointerExited="ExplorerTag_PointerExited"
                                        Padding="6,1" Background="Transparent" BorderThickness="0"
                                        Opacity="0.45" VerticalAlignment="Center"
                                        ToolTipService.ToolTip="{x:Bind ((local:ExplorerItem)Content).TagTooltip}">
                                    <TextBlock Text="@" FontSize="13"/>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>

                <!-- Changes tab: the working tree's current state, fed by the same
                     git status call as the bottom status strip. Read-only by design —
                     staging/committing belongs to the agent or a real git client. Rows
                     tag and drag exactly like file-tree rows; double-click opens. -->
                <ListView x:Name="ChangesList" Grid.Row="2" Visibility="Collapsed"
                          SelectionMode="Single" IsItemClickEnabled="False"
                          CanDragItems="True" CanReorderItems="False" AllowDrop="False"
                          DragItemsStarting="ChangesList_DragItemsStarting"
                          DoubleTapped="ChangesList_DoubleTapped"
                          Padding="4,2,4,6">
                    <!-- No row entrance/exit animations: this list legitimately rebuilds while
                         external tools churn files (builds, scripts), and animated rows read
                         as flicker. Updates apply as instant, still repaints. -->
                    <ListView.ItemContainerTransitions>
                        <TransitionCollection/>
                    </ListView.ItemContainerTransitions>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="MinHeight" Value="28"/>
                            <Setter Property="Padding" Value="8,2"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:GitChangeItem">
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="14"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Kind}" Foreground="{x:Bind KindBrush}"
                                           FontFamily="Cascadia Code,Consolas" FontWeight="Bold"
                                           FontSize="12" VerticalAlignment="Center"
                                           ToolTipService.ToolTip="{x:Bind KindLabel}"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="7">
                                    <TextBlock Text="{x:Bind Name}" FontSize="13"
                                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{x:Bind Dir}" FontSize="11" Opacity="0.45"
                                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <!-- Row actions, view-before-act: ± view diff, ↩ undo, @ tag. -->
                                <Button Grid.Column="2" Click="ChangesDiff_Click"
                                        PointerEntered="ExplorerTag_PointerEntered"
                                        PointerExited="ExplorerTag_PointerExited"
                                        Padding="6,1" Background="Transparent" BorderThickness="0"
                                        Opacity="0.45" VerticalAlignment="Center"
                                        ToolTipService.ToolTip="View diff in the transcript">
                                    <TextBlock Text="±" FontSize="13" FontFamily="Cascadia Code,Consolas"/>
                                </Button>
                                <!-- Undo changes: restores the file to the last commit (asks
                                     first — this is the app's one destructive button). Restores
                                     deleted files too. Hidden for untracked/renamed rows. -->
                                <Button Grid.Column="3" Click="ChangeUndo_Click"
                                        PointerEntered="ExplorerTag_PointerEntered"
                                        PointerExited="ExplorerTag_PointerExited"
                                        Padding="6,1" Background="Transparent" BorderThickness="0"
                                        Opacity="0.45" VerticalAlignment="Center"
                                        Visibility="{x:Bind UndoVisibility}"
                                        ToolTipService.ToolTip="{x:Bind UndoTooltip}">
                                    <FontIcon Glyph="&#xE7A7;" FontSize="12"/>
                                </Button>
                                <Button Grid.Column="4" Click="ExplorerTag_Click"
                                        PointerEntered="ExplorerTag_PointerEntered"
                                        PointerExited="ExplorerTag_PointerExited"
                                        Padding="6,1" Background="Transparent" BorderThickness="0"
                                        Opacity="0.45" VerticalAlignment="Center"
                                        ToolTipService.ToolTip="{x:Bind TagTooltip}">
                                    <TextBlock Text="@" FontSize="13"/>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock x:Name="ChangesEmptyText" Grid.Row="2" Visibility="Collapsed"
                           Text="No changes — working tree is clean"
                           FontSize="12" Opacity="0.5" TextWrapping="Wrap"
                           HorizontalAlignment="Center" Margin="12,24,12,0"/>

                <!-- Commit… never commits by itself: it pre-fills the prompt (like tagging),
                     the user sends it, the agent writes the message and runs git commit, and
                     the bottom-bar command approval gates the exact command. One commit path;
                     the human control is the approval, not a second button. -->
                <Border x:Name="ChangesFooter" Grid.Row="3" Visibility="Collapsed" Padding="10,8"
                        BorderBrush="{StaticResource MandoBorderBrush}" BorderThickness="0,1,0,0">
                    <Button x:Name="CommitButton" HorizontalAlignment="Stretch" Click="Commit_Click"
                            Style="{StaticResource AccentButtonStyle}"
                            ToolTipService.ToolTip="Ask the agent to commit these changes — you'll approve the exact command before anything runs">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE73E;" FontSize="13"/>
                            <TextBlock Text="Commit…"/>
                        </StackPanel>
                    </Button>
                </Border>
            </Grid>
        </Grid>

        <StackPanel x:Name="PlanProgressPanel" Grid.Row="2" Visibility="Collapsed" Padding="16,6,16,0">
            <TextBlock x:Name="PlanProgressText" FontSize="12" Opacity="0.7" Margin="0,0,0,2"/>
            <ProgressBar x:Name="PlanProgressBar" Minimum="0" Maximum="100" Height="4"
                         Foreground="{StaticResource MandoAccentBrush}"/>
        </StackPanel>

        <StackPanel x:Name="BusyPanel" Grid.Row="3" Orientation="Horizontal" Spacing="10"
                    Padding="16,8,16,0" Visibility="Collapsed">
            <ProgressRing x:Name="BusyRing" IsActive="False" Width="18" Height="18"
                          Foreground="{StaticResource MandoAccentBrush}"/>
            <TextBlock x:Name="BusyText" Text="Thinking..." VerticalAlignment="Center"
                       Opacity="0.75" FontSize="13"/>
            <TextBlock Text="(Esc to cancel)" VerticalAlignment="Center" Opacity="0.4" FontSize="12"/>
        </StackPanel>

        <!-- ======= Create-snapshot offer (opt-in) =======
             Slides down from the top of the transcript when a model switch (or "Take snapshot")
             buffers a conversation. Two stages: a thin notification bar, then (on Create) the full
             name + model picker. Non-blocking — keep prompting; it persists until you create or
             dismiss. Grid.Row="1" + Top alignment floats it over the top of the WebView transcript;
             declared after it so it layers on top. -->
        <Grid x:Name="SnapshotOfferRoot" Grid.Row="1" Visibility="Collapsed"
              VerticalAlignment="Top" Margin="16,10,16,0">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="SnapshotOfferTransform"/>
            </Grid.RenderTransform>

            <!-- Stage 1: thin notification bar -->
            <Border x:Name="SnapshotNotifyBar" Padding="12,8" CornerRadius="10"
                    Background="{StaticResource MandoPanelBrush}"
                    BorderBrush="{StaticResource MandoBorderBrush}" BorderThickness="1">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xE722;" FontSize="14" VerticalAlignment="Center"
                              Foreground="{StaticResource MandoAccentBrush}"/>
                    <TextBlock x:Name="SnapshotNotifyText" Grid.Column="1" VerticalAlignment="Center"
                               FontSize="13" TextTrimming="CharacterEllipsis"/>
                    <!-- Model switches only (hidden for manual snapshot offers): continue the
                         SAME conversation on the new model, verbatim — memory, not a recap. -->
                    <Button x:Name="SnapshotKeepMemoryButton" Grid.Column="2" Content="Keep memory"
                            Padding="12,4" Visibility="Collapsed" Click="SnapshotKeepMemory_Click"
                            Style="{StaticResource AccentButtonStyle}"
                            ToolTipService.ToolTip="Continue this conversation on the new model with full memory — nothing summarized, nothing lost"/>
                    <Button Grid.Column="3" Content="Snapshot" Padding="12,4"
                            Click="SnapshotNotifyCreate_Click"
                            ToolTipService.ToolTip="Save a summarized, portable recap you can import into any agent"/>
                    <Button Grid.Column="4" Click="SnapshotOfferDismiss_Click" Padding="6"
                            Background="Transparent" BorderThickness="0"
                            ToolTipService.ToolTip="Dismiss — the conversation won't be saved">
                        <FontIcon Glyph="&#xE711;" FontSize="12"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Stage 2: full name + model picker (shown after Create on the bar) -->
            <Border x:Name="SnapshotOfferCard" Visibility="Collapsed" Padding="14,12" CornerRadius="10"
                    Background="{StaticResource MandoPanelBrush}"
                    BorderBrush="{StaticResource MandoBorderBrush}" BorderThickness="1">
                <Grid>
                <StackPanel x:Name="SnapshotOfferContent" Spacing="10">
                    <Grid>
                        <StackPanel Spacing="2">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE722;" FontSize="14" VerticalAlignment="Center"
                                          Foreground="{StaticResource MandoAccentBrush}"/>
                                <TextBlock Text="Create a context snapshot?" FontWeight="SemiBold" FontSize="13"/>
                            </StackPanel>
                            <TextBlock x:Name="SnapshotOfferSubtitle" Opacity="0.6" FontSize="12"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                        <Button Click="SnapshotOfferDismiss_Click" Padding="6" Background="Transparent"
                                BorderThickness="0" HorizontalAlignment="Right" VerticalAlignment="Top"
                                ToolTipService.ToolTip="Dismiss — the conversation won't be saved">
                            <FontIcon Glyph="&#xE711;" FontSize="12"/>
                        </Button>
                    </Grid>
                    <!-- Name (optional) and the summarizer model share one row: [name] [model] [Create]. -->
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="SnapshotNameBox" Grid.Column="0" PlaceholderText="Name (blank = AI names it)"
                                 MaxLength="80" VerticalAlignment="Center"/>
                        <ComboBox x:Name="SnapshotModelCombo" Grid.Column="1" HorizontalAlignment="Stretch"
                                  VerticalAlignment="Center"
                                  ToolTipService.ToolTip="Model that will write the recap">
                            <ComboBox.ItemTemplate>
                                <DataTemplate x:DataType="local:ModelChoice">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center"/>
                                        <Border CornerRadius="5" Padding="6,1" VerticalAlignment="Center"
                                                Background="{StaticResource MandoBackgroundBrush}">
                                            <TextBlock Text="{x:Bind Tag}" FontSize="10" Opacity="0.75"/>
                                        </Border>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <Button x:Name="SnapshotCreateButton" Grid.Column="2" Content="Create"
                                Click="SnapshotCreate_Click" VerticalAlignment="Center"
                                Style="{StaticResource AccentButtonStyle}"/>
                    </Grid>
                </StackPanel>

                <!-- Busy overlay while the model writes the recap: content fades behind a spinner
                     and contextual text ("Creating …snapshot"). -->
                <StackPanel x:Name="SnapshotBusyPanel" Visibility="Collapsed" Orientation="Horizontal"
                            Spacing="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressRing x:Name="SnapshotBusyRing" IsActive="False" Width="20" Height="20"
                                  Foreground="{StaticResource MandoAccentBrush}"/>
                    <TextBlock x:Name="SnapshotBusyText" VerticalAlignment="Center" FontSize="13"
                               FontWeight="SemiBold"/>
                </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ======= Drop-to-tag overlay =======
             Mounts over the transcript row the moment an in-progress drag enters the tab —
             via ChatRoot's DragEnter for drags over XAML chrome, or the transcript script's
             'drag-enter' web message for drags over the WebView surface (Chromium owns those;
             XAML never sees them). Once visible, the OS retargets the drag to THIS element,
             so the drop arrives in XAML with real file paths. The inner card is hit-test
             invisible so the whole sheet is one uniform drop target. -->
        <Grid x:Name="DropOverlay" Grid.Row="1" Visibility="Collapsed" Background="#B0000000"
              AllowDrop="True" DragOver="DropOverlay_DragOver" Drop="DropOverlay_Drop"
              DragLeave="DropOverlay_DragLeave">
            <Border IsHitTestVisible="False" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Background="{StaticResource MandoPanelBrush}"
                    BorderBrush="{StaticResource MandoAccentBrush}" BorderThickness="2"
                    CornerRadius="14" Padding="36,26">
                <StackPanel Spacing="6" HorizontalAlignment="Center">
                    <TextBlock Text="@" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center"
                               Foreground="{StaticResource MandoAccentBrush}"/>
                    <TextBlock Text="Drop to tag in prompt" FontSize="16" FontWeight="SemiBold"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Files and folders in the project become @references"
                               FontSize="12" Opacity="0.6" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ======= Plan approval bar =======
             Slides up above the input when the assistant proposes a plan. Unlike the centered
             approval modal, it does NOT cover the transcript — the plan card stays readable above
             while you choose. Gates the turn (it's waiting on you) without hiding the plan. -->
        <Border x:Name="PlanApprovalBar" Grid.Row="4" Visibility="Collapsed"
                Margin="16,0,16,10" Padding="14,12" CornerRadius="12"
                Background="{StaticResource MandoPanelBrush}"
                BorderBrush="{StaticResource MandoAccentBrush}" BorderThickness="1">
            <Border.RenderTransform>
                <TranslateTransform x:Name="PlanApprovalTransform"/>
            </Border.RenderTransform>
            <StackPanel Spacing="10">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE8FD;" FontSize="15" VerticalAlignment="Center"
                              Foreground="{StaticResource MandoAccentBrush}"/>
                    <TextBlock x:Name="PlanApprovalTitle" FontWeight="SemiBold" FontSize="13"
                               VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
                <!-- Command text for bottom-bar command approvals (collapsed for plan approvals). -->
                <TextBlock x:Name="PlanApprovalCommand" Visibility="Collapsed" TextWrapping="Wrap"
                           FontFamily="Cascadia Code,Cascadia Mono,Consolas" FontSize="13"
                           Foreground="{StaticResource MandoSkyBrush}" Opacity="0.95"/>
                <!-- Wraps only when the window is too narrow for all options in one row. -->
                <controls:WrapPanel x:Name="PlanApprovalButtons" HorizontalSpacing="8" VerticalSpacing="8"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="5" Padding="16,10,16,14" ColumnSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border x:Name="SuggestionsPanel" Grid.Row="0" Grid.ColumnSpan="3" Visibility="Collapsed"
                    Background="{StaticResource MandoPanelBrush}"
                    BorderBrush="{StaticResource MandoBorderBrush}" BorderThickness="1"
                    CornerRadius="8" Margin="0,0,0,8" MaxHeight="220">
                <ListView x:Name="SuggestionsList" SelectionMode="Single"
                          IsItemClickEnabled="True" ItemClick="SuggestionsList_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:CommandSuggestion">
                            <StackPanel Orientation="Horizontal" Spacing="12" Padding="0,2">
                                <TextBlock Text="{x:Bind Command}" FontFamily="Cascadia Code,Consolas"
                                           Foreground="{StaticResource MandoSkyBrush}" MinWidth="140"/>
                                <TextBlock Text="{x:Bind Description}" Opacity="0.6" FontSize="12"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <TextBox x:Name="InputBox" Grid.Row="1" Grid.Column="0"
                     PlaceholderText="Message MandoCode…   /commands · @file · !shell"
                     AcceptsReturn="True" TextWrapping="Wrap" MaxHeight="140"
                     AllowDrop="True" DragOver="InputBox_DragOver" Drop="InputBox_Drop"
                     PreviewKeyDown="InputBox_PreviewKeyDown" TextChanged="InputBox_TextChanged"/>

            <Button x:Name="EmojiButton" Grid.Row="1" Grid.Column="1"
                    VerticalAlignment="Stretch" Padding="10,0"
                    Background="Transparent" BorderThickness="0"
                    ToolTipService.ToolTip="Insert emoji — or type :fire: shortcodes (Win + . opens the full Windows picker)">
                <FontIcon Glyph="&#xE76E;" FontSize="16"/>
                <Button.Flyout>
                    <Flyout Placement="TopEdgeAlignedRight">
                        <StackPanel Width="312" Spacing="6">
                            <GridView x:Name="EmojiGrid" SelectionMode="None" IsItemClickEnabled="True"
                                      ItemClick="EmojiGrid_ItemClick" MaxHeight="248"
                                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="x:String">
                                        <TextBlock Text="{x:Bind}" FontFamily="Segoe UI Emoji" FontSize="20"
                                                   Width="30" Height="30" TextAlignment="Center"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                            <TextBlock Text="Tip: type :fire: style shortcodes in the input box for suggestions, or press Win + . for the full Windows emoji picker"
                                       FontSize="11" Opacity="0.6" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>

            <Button x:Name="SendButton" Grid.Row="1" Grid.Column="2" Click="SendButton_Click"
                    VerticalAlignment="Stretch" MinWidth="88"
                    Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon x:Name="SendIcon" Glyph="&#xE724;" FontSize="14"/>
                    <TextBlock x:Name="SendLabel" Text="Send"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- ======= Status strip =======
             Slim VS Code-style strip, bottom right: [branch glyph][state dot][branch ↑↓].
             The glyph is drawn inline (no git-branch glyph exists in the Segoe icon fonts);
             the dot is the one status light: red = merge conflicts, gold = uncommitted
             changes, green = clean. Hidden entirely when the folder isn't a git repo. -->
        <Grid x:Name="StatusStrip" Grid.Row="6" Padding="16,0,16,6" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" Spacing="7" HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        ToolTipService.ToolTip="Local git status for this tab's project folder">
                <Canvas Width="14" Height="14" VerticalAlignment="Center" Opacity="0.7">
                    <!-- classic branch shape: main line with two nodes, side node joined by a curve -->
                    <Ellipse Canvas.Left="1.5" Canvas.Top="0.5" Width="4" Height="4"
                             Stroke="{StaticResource MandoDimBrush}" StrokeThickness="1.2"/>
                    <Ellipse Canvas.Left="1.5" Canvas.Top="9.5" Width="4" Height="4"
                             Stroke="{StaticResource MandoDimBrush}" StrokeThickness="1.2"/>
                    <Ellipse Canvas.Left="8.5" Canvas.Top="0.5" Width="4" Height="4"
                             Stroke="{StaticResource MandoDimBrush}" StrokeThickness="1.2"/>
                    <Path Stroke="{StaticResource MandoDimBrush}" StrokeThickness="1.2"
                          Data="M 3.5,4.5 L 3.5,9.5 M 10.5,4.5 C 10.5,8 3.5,6.5 3.5,9.5"/>
                </Canvas>
                <Ellipse x:Name="BranchDot" Width="7" Height="7" VerticalAlignment="Center"
                         Fill="{StaticResource MandoGreenBrush}"/>
                <TextBlock x:Name="BranchText" FontSize="12" Opacity="0.8" VerticalAlignment="Center"
                           FontFamily="Cascadia Code,Consolas"/>
            </StackPanel>
        </Grid>

        <!-- ======= Approval overlay — this tab only.
             Each tab owns its WinUiApprovalService, ApprovalPromptGate and McpApprovalGate,
             so two agents can sit at an approval prompt simultaneously without one blocking
             the other's overlay from ever rendering. ======= -->
        <Grid x:Name="ApprovalOverlay" Grid.RowSpan="7" Visibility="Collapsed"
              Background="#B0000000">
            <Border x:Name="ApprovalCard" MaxWidth="860" MaxHeight="640" Margin="24" Padding="20"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Background="{StaticResource MandoPanelBrush}"
                    BorderBrush="{StaticResource MandoAccentBrush}" BorderThickness="1"
                    CornerRadius="12">
                <Grid RowSpacing="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="ApprovalTitle" Grid.Row="0" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource MandoSkyBrush}" TextWrapping="Wrap"/>
                    <TextBlock x:Name="ApprovalSubtitle" Grid.Row="1" FontSize="13"
                               FontFamily="Cascadia Code,Consolas" Opacity="0.8"
                               TextWrapping="Wrap" Visibility="Collapsed"/>
                    <TextBlock x:Name="ApprovalDetail" Grid.Row="2" FontSize="13" Opacity="0.7"
                               TextWrapping="Wrap" Visibility="Collapsed"/>

                    <ScrollViewer x:Name="ApprovalBodyScroll" Grid.Row="3" Visibility="Collapsed"
                                  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                                  Background="{StaticResource MandoBackgroundBrush}" CornerRadius="8"
                                  Padding="10" MaxHeight="320">
                        <ItemsControl x:Name="ApprovalDiffList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="local:DiffLineVm">
                                    <TextBlock Text="{x:Bind Text}" Foreground="{x:Bind Brush}"
                                               FontFamily="Cascadia Code,Cascadia Mono,Consolas"
                                               FontSize="13"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <StackPanel Grid.Row="4" Spacing="8">
                        <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="ApprovalButtons" Spacing="6"/>
                        </ScrollViewer>
                    </StackPanel>

                    <!-- Instruction input spans the body + footer rows so the text box
                         fills the enlarged card instead of hugging the bottom edge. -->
                    <Grid x:Name="InstructionPanel" Grid.Row="3" Grid.RowSpan="2"
                          Visibility="Collapsed" RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="InstructionPrompt" FontSize="13" Opacity="0.8"
                                   TextWrapping="Wrap"/>
                        <TextBox x:Name="InstructionBox" Grid.Row="1" AcceptsReturn="True"
                                 TextWrapping="Wrap" VerticalAlignment="Stretch"
                                 VerticalContentAlignment="Top"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 PreviewKeyDown="InstructionBox_KeyDown"
                                 PlaceholderText="Type your answer — Enter to submit, Shift+Enter for a new line"/>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
                            <Button Content="Submit" Click="InstructionSubmit_Click"
                                    Style="{StaticResource AccentButtonStyle}"/>
                            <Button x:Name="InstructionCancelButton" Content="Cancel"
                                    Click="InstructionCancel_Click" Visibility="Collapsed"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
